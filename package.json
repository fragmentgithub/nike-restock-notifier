{
  "name": "nike-restock-notifier",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check scripts/monitor.js && node --check scripts/check-health.js && node --check scripts/recover-monitor.js && node --check scripts/serve-pages.js && node --check scripts/test-discord.js && node --check public/app.js && node --check src/worker.js && node --check scripts/cloudflare-admin.js && node --check scripts/import-cloudflare-webhook.js && node --check scripts/export-cloudflare-state.js",
    "discord:test": "node scripts/test-discord.js",
    "health": "node scripts/check-health.js",
    "monitor": "node scripts/monitor.js",
    "start": "node scripts/serve-pages.js",
    "test": "npm run check && node --test \"test/**/*.test.js\"",
    "cloudflare:build": "wrangler deploy --dry-run --outdir dist",
    "cloudflare:deploy": "wrangler deploy",
    "cloudflare:types": "wrangler types",
    "cloudflare:admin": "node scripts/cloudflare-admin.js"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260904.1",
    "wrangler": "^4.129.0"
  }
}
